build/
dist/
# `docs/` used to be ignored here, which silently swallowed every document put
# in it. The interface contract for a Python build lives there now, so the
# directory is tracked. (The `rust-gui` branch has the same line removed for the
# same reason.)
__pycache__/
*.pyc
*.pyo
*.spec
*.log

# IDE / tooling
.claude/
.workbuddy/

# Rust local build output and cache
target/
**/target/
.cargo/
**/.cargo/
*.rs.bk
.codex/
.commandcode/
